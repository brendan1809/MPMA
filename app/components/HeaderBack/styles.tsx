import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
  container: {
    alignItems: "center",
    height: 36,
    justifyContent: "center",
    marginLeft: 0,
    width: 36,
  },
})
