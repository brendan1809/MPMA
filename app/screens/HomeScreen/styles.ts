import { StyleSheet } from "react-native"

export const style = StyleSheet.create({
  button: { marginTop: 30 },
  container: {
    alignItems: "center",
    padding: 20,
  },
  flex: { flex: 1 },
  headingText: {
    fontSize: 25,
    marginVertical: 10
  }
})
